engines:
  lichess:
    method: GET
    url: "https://lichess.org/api/cloud-eval"
    params:
      fen: "{fen}"
      multiPv: "1"
    extract: "$.pvs[0].moves"
  stockfish-local:
    type: uci
    command: "stockfish"
